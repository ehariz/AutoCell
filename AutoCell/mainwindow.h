#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>

class mainWindow : public QMainWindow
{
    Q_OBJECT
public:
    explicit mainWindow(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // MAINWINDOW_H